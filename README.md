# Discourse Super Navigation Suite

This workspace now contains two deliverables:

- `discourse-super-navigation-suite` (plugin core, Ruby backend)
- `discourse-super-navigation-suite-theme` (theme component UI, JS/CSS)

Start with `discourse-super-navigation-suite/README.md`, then install `discourse-super-navigation-suite-theme`.
